{
  "name": "server",
  "version": "1.0.0",
  "description": "- save contract last processed blockNumber - in appjs create a callback func that executes the logic every n amount of secs - coul use contract.on bc we are only listening to one event",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "start": "node index.js",
    "dev": "npm run reset-data && npx nodemon index.js",
    "reset-data": "node resetdb.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.12.0",
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "eslint": "^7.22.0",
    "ethers": "^5.7.2",
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "mongoose": "^5.12.1",
    "nodemon": "^2.0.12",
    "prettier": "^2.2.1",
    "prisma": "^4.12.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0"
  }
}
